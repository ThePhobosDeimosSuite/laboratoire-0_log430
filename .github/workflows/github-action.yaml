name: CI
on: push
jobs:
  pipeline:
    runs-on: ubuntu-latest
    steps:
      - run: echo "npm install"
      - run: npm ci
      - run: echo "ESLint" 
      - run: npx eslint
      - run: echo "Unit test"
      - run: npm test
